# Portfolio Watch — 一页纸

**交付物是信任，不是仪表盘。** 一个 portfolio watch 能留在用户手机上，
前提是"没响 = 没事"始终成立。所以整个 skill 围绕一条不变量构建——
**告警通道的信噪比钉在 100% 附近**——阈值、界面、降级、分享全部由它推导。

## 我对题目的理解

"任何 Alva 用户"是两个分布的乘积，不是一个。**入口分布**：已连账户 /
申报持仓 / 只报 ticker / 只是问一句。**资产分布**：题目自己的例句就是
NVDA、TSLA、AAPL——美股——而 Alva 上最自然的首建是 crypto。因此 skill
拆为资产无关的核心（管线、告警纪律、界面契约、送达验证）+ 按类别模块
（`asset-equity.md`、`asset-crypto.md`），来源层有**三种一等模式**：
连接账户、申报持仓、纯 watchlist。题目原句**零阻塞问题**直接构建——
ticker 本身就是可构建的组合，数量能解锁什么只是总结里的一行升级提示，
绝不是前置条件。

## 撑起产品的五个决策

1. **风险的单位是敞口，不是代码。** 收益对用户自己持有的基准分解为
   β 与残差：相关持仓同向波动是**一次因子事件 → 一条告警**；单票只在
   **残差**波动（减掉市场后它自己的新闻）超阈时发声。集中度用
   **有效押注数**（1/Σw²）度量——五个持仓可能只有 1.8 个独立押注，
   这个数字才是重仓者真正需要看到的。
2. **"大动静"相对资产自身定义，不用固定百分比。** 阈值按 σ 标定
   （股票用 20 个**交易日**，crypto 用日历日）；残差波动按残差 σ 标定。
3. **每条告警指明它服务的决策**——用户自己设的再平衡带、待验证的
   论点、风险限额——绝不给交易指令。信息只有能改变行动才值得打断。
   只报状态变化、仅升级重报、每次运行一条 digest。
4. **休市是特性，不是数据过期。** 股票有会话语义（跳空开盘判一次、
   量能收盘判、周末显示 "market closed"）；混合组合跑**一个时钟**，
   类别判断窗口在 producer 内部。
5. **规则必须可证伪。** 12 个月确定性告警回放直接渲染在页面上：
   哪些天会响、其余日子是否真的安静。回放已经立功——上线前暴露出
   带边震荡（年 58 个告警日），加入滞回后降到 31（约 2.6 条/月、
   88% 的日子安静）。"沉默即信息"是测出来的，不是宣称的。

## 证据

- **行为评测**（决策级断言 + 证据引用评分，只收紧不放松）：v3
  **63/63**，基线 42/63；v2 在受重构影响场景 13/19 vs 19/19——
  equity 模块与 watchlist 模式是承重墙。iteration 4 新增重仓组合
  场景验证 v4 敞口规则。
- **同一个 skill 的三次真机构建，覆盖其全部输入谱**：连接账户的
  crypto watch（share-safe 公开镜像、送达已证明）、题目原句的纯
  watchlist（真实财报日告警、深链按钮）、重仓组合 showcase（有效
  押注 3.3/4、相关性 0.73、β/残差列、页面内嵌实时回放面板）。
  全部 lint 全绿、全部实时读数、全部已发布。

## 指标与下一步

北极星：**Weekly Trusted Watches**——周活跃**且未静音**的 watch 数；
被静音的 watch 是尚未发生的流失。护栏：告警打开率（先于静音率下滑）、
stale 运行占比、无来源数字数（恒为 0）。下一步：每条告警的"有用/噪音"
反馈闭环反哺阈值；合约/杠杆作为独立模块（自带清算距离告警）；
把高留存参数组合经 Remix 沉淀为 blueprint。
