# Portfolio Watch Skill — 做题思路

用户说一句"盯下我的 NVDA、TSLA、AAPL，有大事提醒我"，这个 Skill 要产出
的不只是一个页面，而是一份长期替他盯钱的判断力：生成的 Playbook 每个交易
日运行几十次，每一次都要正确回答同一个问题："现在有没有值得告诉用户的
事？"这个判断错的代价是不对称的：错发一条假警报，用户从此不再相信通知；
漏掉一次真风险，用户直接亏钱。

所以我的做法是，把"什么值得打扰一个投资者"的完整判断标准写成 AI 可以严
格执行的方法论，让它对任何持仓都成立，并且可以拿历史数据检验。展开来是
三个问题，也是这页纸的三个部分：**信什么，说什么，凭什么信**。

## 一、信什么：持仓千变万化，真相只有一套规则

一个从没见过的持仓，变化的其实只有两件事：**持仓从哪来**，和**里面是什
么资产**。

- **来源有三种，三种都直接开工**：连了交易账户（数据最真）；口头报持仓
  （贴一张券商截图也行，现金也可以报）；或者只说几个代码。只给代码就照
  建，一个问题都不追问。缺了数量只是暂时没有净值和回撤，页面会写明缺什
  么，用户随时补一句"我有 20 股 NVDA"就能解锁。
- **资产类别做成插件**：美股、crypto、A 股各自带交易时段、计价方式和特
  有事件（财报、跳空、涨跌停、脱锚），而取数、判断、告警、页面这条主线
  一行不改。支持一类新资产是加一个模块，不是改一遍产品。

在这之上是一条铁律：页面上每个数字都能说出自己从哪个接口、什么时间来。
AI 不许编数；取不到价就标"过期"并且闭嘴。

## 二、说什么：只在值得的时候打扰

**看什么？** 风险从近到远分四层看：

1. **单个资产**：价格异动、重大新闻、财报、跳空；
2. **资产之间**：几只相关的股票一起跌，是一件事，不是几件——先剥掉大盘
   的影响，剩下的部分才算这只股票自己的新闻；
3. **组合整体**：回撤、集中度、偏离用户自设的目标仓位。集中度不数持仓
   个数，算"有效独立注数"：五只 AI 股可能只等于 1.8 个独立赌注；
4. **系统本身**：账户断连了，也要告诉用户一声（且只说一声）。

**多大算大事？** 和这只资产自己的波动水平比，不设统一百分比：跌 5% 对中
盘股是家常便饭，对稳定币是天塌了。而且只认"跨档"：回撤从 9% 走到 11%
是进了新区间，说一次；在 10% 附近来回晃，不说。

**什么不说？** 四类噪音，逐类压掉：

- 说过的：同一个状态只通知一次，恶化了才再说，好转默默更新页面；
- 来回晃的：刚通知过的状态，短时间内反复穿越阈值，不重复打扰；
- 大盘带的：指数跌 3% 带着个股跌 3%，那是市场新闻，不冒充个股新闻；
- 数据坏的：接口出错宁可标"数据过期"，绝不拿坏数据响警报。

压完之后，告诉用户一句他能核对的话："普通灵敏度下，大约每周一到两条。"
拿过去一年的行情回测，82% 的交易日确实一声不响。

**几件事同时发生怎么办？** 先合并再排序：板块一起动就发一条板块提醒，
不发五条个股；一次运行最多发一条摘要，里面按轻重排列。

**响了之后呢？** 每条告警带一个"打开 Playbook"按钮，点开直达页面上对应
的内容。告警是门铃，页面才是完整的答案。

## 三、凭什么信：每个说法在平台上都有对应的事实

**这份 skill 在 Alva 上真实跑出了三个 Playbook**，故意选了三种完全不同
的输入：一个连账户的币组合，一个只报三个代码的美股 watchlist，一个重仓
单只 A 股、带目标仓位和现金的申报账本。三个都发布在线上，页面实时读数
（链接见 SUBMISSION）。

**该响的响了，不该响的没响。** NVDA 财报次日，美股 watch 发出三条告警：
盘中异动、异动升级、收盘确认，每条在平台送达记录里都能查到"已送达"。同
一时期，A 股 watch 运行了 29 次，一声没吭——因为确实没事。

**规则被自己的回放抓过三次错。** 页面上内嵌了过去 12 个月的规则回放，它
先后暴露了三处设计缺陷：阈值边缘反复报警、未复权数据把除权日误判成暴
跌、回放代码悄悄偏离了线上行为。三次修完，线上、回放、离线测试现在共用
同一份判断代码。"没响就是没事"不是口号，是回放测出来的数字。

**平台用法逐条核实过**，包括和平台第一方 portfolio watch 的数据模型逐字
段对照。对照的结果印证了两个关键设计：只报代码不报数量是正式输入，不是
残缺输入；确认送达要查两件事，用户开没开，和通道存不存在。

**AI 执行得怎么样，考过四轮。** 用真实用户话术出题（包括"只给代码别反
问""嫌太吵"这类容易做错的场景），检查 AI 按这份 skill 做出的每个决策：
63 项全部通过；同样的题不装 skill 裸跑，只对 42 项。

## 四、边界与下一步

北极星指标是**每周活跃且没被静音的 watch 数**：被静音的 watch，是还没
发生的流失。两条边界如实写在产品里：口头申报的持仓暂不支持做空和外币；
申报清单要用户自己维护，页面会标注"截至某日申报"。下一步做两件事：给每
条告警加"有用 / 噪音"反馈，回收进阈值；把合约和杠杆做成新模块，自带清
算距离提醒。
