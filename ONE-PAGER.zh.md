# Portfolio Watch Skill — 做题思路

用户说一句"盯下我的 NVDA、TSLA、AAPL，有大事提醒我"，这个 Skill 要产出
的不只是一个页面，而是一份长期替他盯钱的判断力：生成的 Playbook 每个交易
日运行几十次，每一次都要正确回答同一个问题："现在有没有值得告诉用户的
事？"这个判断错的代价是不对称的：错发一条假警报，用户从此不再相信通知；
漏掉一次真风险，用户直接亏钱。

所以我的做法是，把"什么值得打扰一个投资者"的完整判断标准写成 AI 可以严
格执行的方法论，让它对任何持仓都成立，并且可以拿历史数据检验。这套方法
论分三层展开，也构成这页纸的结构：**事实层**（数据从哪来、信到什么程
度）、**判断层**（什么值得通知用户）、**验证层**（设计如何被证实）。

## 一、事实层：持仓千变万化，数据规则只有一套

先交代生成的 Playbook 是什么：一条定时运行的流水线，每次运行做四件事——
**取行情，算指标，判断有没有值得说的事，然后更新页面、必要时推送通知**。
一个从没见过的持仓，能变的只有流水线的输入：**持仓从哪来**，和**里面是
什么资产**。

- **来源有三种，三种都直接开工**：连了交易账户（数据最真）；口头报持仓
  （贴一张券商截图也行，现金也可以报）；或者只说几个代码。只给代码就照
  建，一个问题都不追问。缺了数量只是暂时没有净值和回撤，页面会写明缺什
  么，用户随时补一句"我有 20 股 NVDA"就能解锁。
- **资产类别做成插件**：美股、crypto、A 股各自的差异，归纳起来只有两块
  ——从哪取行情，以及判断哪些特有事件（财报、跳空、涨跌停、脱锚）。换
  一类资产只换这两块，流水线的四个环节不动。所以支持一类新资产是加一个
  模块，不是改一遍产品。

无论持仓来自哪种来源，同一条铁律都成立：**页面上的每个数字，都来自一次
真实的接口调用，并带着取数时间**。保证它靠三个做法：页面每次打开都现场
读取数据，数字从不写死在网页里；AI 只做文字性判断（比如一条新闻重不重
要），所有价格、市值、盈亏都由代码从接口取回；接口取不到数时，对应资产
标"数据过期"并暂停它的告警，绝不拿坏数据说事。

## 二、判断层：只在值得的时候通知

**监控哪些维度？** 风险从近到远分四层看：

1. **单个资产**：价格异动、重大新闻、财报、跳空；
2. **资产之间**：几只相关的股票一起跌，是一件事，不是几件——先剥掉大盘
   的影响，剩下的部分才算这只股票自己的新闻；
3. **组合整体**：回撤、集中度、偏离用户自设的目标仓位。集中度不数持仓
   个数，算"有效独立注数"：五只 AI 股可能只等于 1.8 个独立赌注；
4. **系统本身**：账户断连了，也要告诉用户一声（且只说一声）。

**多大的变化算异动？** 和这只资产自己的波动水平比，不设统一百分比：跌 5% 对中
盘股是家常便饭，对稳定币是天塌了。而且只认"跨档"：回撤从 9% 走到 11%
是进了新区间，说一次；在 10% 附近来回晃，不说。

**哪些情况不通知？** 四类噪音，逐类压掉：

- 说过的：同一个状态只通知一次，恶化了才再说，好转默默更新页面；
- 来回晃的：刚通知过的状态，短时间内反复穿越阈值，不重复打扰；
- 大盘带的：指数跌 3% 带着个股跌 3%，那是市场新闻，不冒充个股新闻；
- 数据坏的：接口出错宁可标"数据过期"，绝不拿坏数据响警报。

压完之后，告诉用户一句他能核对的话："普通灵敏度下，大约每周一到两条。"
拿过去一年的行情回测，82% 的交易日确实一声不响。

**多个信号同时出现，如何处理？** 先合并再排序：板块一起动就发一条板块
提醒，不发五条个股；一次运行最多发一条摘要，里面按轻重排列。

**通知送达之后？** 每条告警带一个"打开 Playbook"按钮，点开直达页面上对
应的内容。告警是门铃，页面才是完整的答案。

## 三、验证层：每个设计主张都经过现实检验

前两部分是设计。这一部分回答一个问题：这些设计有没有被现实检验过？四个
主张，各有一份发生在真实 Alva 平台上的证据。

- **"对任何持仓都成立"**——沿"来源 × 资产"两个维度取了四个完全不同的
  组合，各构建了一个真实 Playbook，全部发布在线上、页面实时读数：
  [连账户的币组合](https://alva.ai/u/lx79d/playbooks/portfolio-watch-demo-safe)、
  [只报三个代码的美股 watchlist](https://alva.ai/u/lx79d/playbooks/portfolio-watch-equity-demo)、
  [重仓单只 A 股并带目标仓位和现金的申报账本](https://alva.ai/u/lx79d/playbooks/portfolio-watch-showcase)、
  [申报持仓的加密账本](https://alva.ai/u/lx79d/playbooks/portfolio-watch-crypto-book)。
- **"只在值得的时候通知"**——被真实行情从两个方向验证。会响：NVDA 财报
  次日，美股 watch 发出三条告警（盘中异动、异动升级、收盘确认），每条
  在平台送达记录中均为"已送达"。会安静：同一时期 A 股 watch 运行 29
  次、零告警，期间该组合确实没有重大变化。此外页面内嵌了过去 12 个月的
  规则回放，它先后暴露了三处设计缺陷（阈值边缘反复报警、未复权数据把除
  权日误判成暴跌、回放代码偏离线上行为）；修复后，线上、回放、离线测试
  共用同一份判断代码。
- **"每个数字都真实"**——skill 中的每一处平台用法都逐条核实过，并与平台
  第一方 portfolio watch 的数据模型做了逐字段对照。对照印证了两个设计选
  择：只报代码不报数量是正式输入，不是残缺输入；确认送达要查两件事——用
  户是否开启，以及通道是否真实存在。
- **"AI 会照着执行"**——做了四轮行为评测：用真实用户话术出题（含"只给代
  码别反问""嫌太吵"这类易错场景），逐项检查 AI 依据这份 skill 做出的决
  策。63 项全部通过；不加载 skill 的对照组通过 42 项。

## 四、上线之后：如何衡量，还差什么

**怎么衡量**：一个值得观察的指标是**每周活跃且未被静音的 watch 数**——
用户静音一个 watch 通常发生在卸载之前，所以"未被静音"比"活跃"更早反映
信任的变化。配合观察告警的打开率（它会先于静音率恶化）；同时守住一条底
线：页面上不允许出现任何说不出数据来源的数字——这就是第一部分那条铁律，
上线后它从设计原则变成需要持续巡检的质量红线。

**当前的边界**（都如实写在产品里）：口头申报的持仓暂不支持做空和外币；
申报清单由用户自己维护，页面标注"截至某日申报"。

**下一步两件事**：给每条告警加"有用 / 噪音"一键反馈，把结果回收进阈值；
把合约和杠杆做成新模块，自带清算距离提醒。
