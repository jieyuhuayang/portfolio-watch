# Portfolio Watch Skill — 做题思路

用户说一句"盯下我的 NVDA、TSLA、AAPL，有大事提醒我"，这个 skill 就把它
变成一个持续运行的 Playbook：一个实时刷新的持仓页面，加一条只在真正有事
时才响的告警通道。整份 skill 的方法论只服务一个承诺：**"没响，就是没事"
必须是用户可以依赖的事实**——因为盯钱的产品，第一条被无视的告警就是流失
的开始。这个承诺拆成三条纪律：页面上每个数字可溯源（AI 永远不许编数）、
每次打扰必须值得（噪音被系统性压制）、这套规则本身可以被历史数据检验
（不是"我觉得合理"，是"回放过去一年，它确实只响了该响的天"）。

## 一、可复用：拆成"不变的方法论 + 可换的模块"

"从没见过的持仓"无非两个变量在变：**持仓从哪来**，以及**里面是什么资产**。

- **来源有三种，三种都直接建**：连了交易账户（数据最真）；口头报持仓
  （支持贴一张券商截图识别、支持申报现金）；或者只报几个代码。题目原句
  只给了三个代码——skill 照建不误，一个追问都没有；缺持仓数量只是少了
  净值和回撤功能，页面会明说缺什么、补一句"我有 20 股 NVDA"就能解锁。
- **资产类别做成插件**：美股、crypto、A 股各带自己的交易时段、计价和
  告警种类（财报、跳空、涨跌停、脱锚），核心管线——取数、判断、告警、
  页面——一行不改。新增一类资产是加一个模块，不是改产品。

同一份 skill 由此造出了三个截然不同的组合（见"证据"），核心零改动。

## 二、界面与告警：题目四问的四个回答

**盯哪些维度？** 按"风险住在哪"分四层：单资产（价格、新闻、财报、
跳空）；组合横截面（几只相关的股票一起跌是**一件事**，不是五件；剔除
大盘影响后剩下的"残差"才是这只股票自己的新闻）；组合整体（回撤、集中
度、偏离用户自设的目标仓位）；系统（账户断连）。集中度用"有效独立注
数"衡量——五只 AI 股可能只等于 1.8 个独立赌注，这个数字比持仓个数更
接近重仓者的真实风险。

**什么算异动？** 相对这只资产自己的波动率来定，不用固定百分比——"跌
5%"对中盘股是家常便饭，对稳定币是天塌了。并且只认"状态跨档"：回撤从
9% 到 11% 是跨进了新区间，响一次；在 10% 边缘来回晃，不响。

**什么是噪音？** 四类，全部显式压掉：说过的（同一状态只通知一次）、
来回晃的（冷却期）、大盘带动却伪装成个股新闻的（剥掉大盘影响再判断）、数据
故障（接口取不到价就标"过期"并闭嘴——用坏数据响一次假警报，信任就没
了）。压完之后向用户翻译成一句可追责的承诺："normal 档大约每周 1–2
条"——回放实测约每月 3.7 个告警日，82% 的交易日安静。

**多个信号同时出现怎么排？** 先合并再排序：板块共振吸收掉当次的单股信
号，残差信号吸收掉原始涨跌信号，然后一次运行**最多发一条摘要**，内部按
严重度排列。只有恶化才会再次打扰；好转默默更新页面。

**告警到手机后**，每条自带"打开 Playbook"深链按钮，点开直达页面上对应
的内容——告警是门铃，页面才是完整的答案。

## 三、证据：三个真机 Playbook + 两个方向的送达验证

- **三次真实构建，覆盖全部输入形态**（链接见 SUBMISSION）：连账户的
  crypto 组合、题目原句的美股 watchlist、重仓中际旭创的 A 股申报账本
  （含涨跌停、除权、午休时段语义）。全部实时读数、已发布。
- **送达在两个方向上都被真实行情验证过**：美股 demo 在 NVDA 财报次日发
  出 3 条真实告警（盘中异动、异动升级、收盘确认），平台送达记录逐条可
  查；A 股 demo 同期 29 次运行零告警——因为确实没事。该响的响了，不该
  响的没响。
- **规则被自己的回放抓过三次错**：12 个月历史回放直接印在页面上，先后
  暴露了阈值边缘反复报警、未复权数据把除权日伪装成暴跌、以及回放实现悄
  悄偏离实盘三处设计缺陷——修复后，实盘、回放、离线测试共用同一份判断
  代码，"沉默即信息"从口号变成了测出来的数字。
- **每条平台用法都逐条核实过**，包括与平台第一方 portfolio watch 的数
  据模型逐字段对照——官方模型印证了本 skill 的两个关键立场：只报代码
  不报数量是一等输入；送达要查"用户开了"和"通道真实存在"两件事。
- **四轮行为评测**：用真实用户话术（含"只给代码别反问""嫌太吵"这类刁
  钻场景）考察 AI 按 skill 执行的效果，63/63 通过；不加载 skill 的裸跑
  基线是 42/63。

## 四、边界与下一步

北极星指标是**每周活跃且未被静音的 watch 数**——被静音的 watch 是还没
发生的流失，所以护栏盯告警打开率和"无来源数字数恒为零"。已声明的边界：
申报模式暂不支持做空和外币持仓（如实告知，不装懂）。下一步：每条告警加
"有用 / 噪音"反馈回收进阈值；合约与杠杆做成新模块，自带清算距离告警。
