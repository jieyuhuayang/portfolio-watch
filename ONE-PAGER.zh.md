# Portfolio Watch — 一页纸

**交付物是信任，不是仪表盘。** 一个 portfolio watch 能留在用户手机上，
前提是"没响 = 没事"始终成立。所以整个 skill 围绕一条不变量构建——
**告警通道的信噪比钉在 100% 附近**——阈值、界面、降级、分享全部由它推导。

## 我对题目的理解

"任何 Alva 用户"是两个分布的乘积，不是一个。**入口分布**：已连账户 /
申报持仓 / 只报 ticker / 只是问一句。**资产分布**：题目自己的例句就是
NVDA、TSLA、AAPL——美股——而 Alva 上最自然的首建是 crypto。因此 skill
拆为资产无关的核心（管线、告警纪律、界面契约、送达验证）+ 按类别模块
（`asset-equity.md`、`asset-crypto.md`），来源层有**三种一等模式**：
连接账户、申报持仓、纯 watchlist。题目原句**零阻塞问题**直接构建——
ticker 本身就是可构建的组合，数量能解锁什么只是总结里的一行升级提示，
绝不是前置条件。

## 撑起产品的四个决策

1. **"大动静"相对资产自身定义，不用固定百分比。** 阈值按 σ 标定
   （股票用 20 个**交易日**，crypto 用日历日）。固定 ±5% 对中盘股
   周周报警、对巨头常年沉默。
2. **只报状态变化，不报状态。** novelty gate（离散状态指纹、仅升级
   重报、震荡冷却）+ 每次运行一条按严重度排序的 digest——这同时是
   平台"每 source 每 run 至多一条"的契约：产品判断与 API 约束同构。
3. **休市是特性，不是数据过期。** 股票有会话语义：跳空只在开盘判一次、
   量能只在收盘判、周末徽章显示"market closed"——每逢周末喊 stale 的
   徽章会教会用户忽略它。混合组合跑在**一个时钟**上：单一 automation
   取 cadence 并集，类别判断窗口在 producer 内部。
4. **绝不编造数字。** watchlist 不合成等权 NAV、不猜成本价、不用 CSS
   隐藏冒充私密。缺数据就可见地降级（carried price → 压制该资产告警 →
   诚实徽章）；分享在数据层剥离绝对值。

## 证据

- **行为评测**（iteration 3，决策级断言 + 证据引用评分）：带 skill
  **63/63**，基线 42/63；v2（crypto-only）在受重构影响的场景 13/19，
  v3 同场景 19/19——equity 模块与 watchlist 模式是承重墙，不是装饰。
- **同一个 skill 的两次真机构建**：crypto watch（Binance + share-safe
  公开镜像）与用题目原句构建的美股 watchlist——两者都有 lint 全绿的
  已发布界面、逐门验证的四环送达链、真实触发下的去重证明、以及
  深链回页面的告警按钮。

## 指标与下一步

北极星：**Weekly Trusted Watches**——周活跃**且未静音**的 watch 数；
被静音的 watch 是尚未发生的流失。护栏：告警打开率（先于静音率下滑）、
stale 运行占比、无来源数字数（恒为 0）。下一步：每条告警的"有用/噪音"
反馈闭环反哺阈值；合约/杠杆作为独立模块（自带清算距离告警）；
把高留存参数组合经 Remix 沉淀为 blueprint。
